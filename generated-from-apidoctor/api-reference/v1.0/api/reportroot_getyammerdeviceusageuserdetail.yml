### YamlMime:GraphOperation
uid: graph.reportroot_getyammerdeviceusageuserdetail
metadata:
  content_git_url: https://github.com/microsoftgraph/microsoft-graph-docs/blob/master/api-reference/v1.0/api/reportroot_getyammerdeviceusageuserdetail.md
summary:
  heading: "reportRoot: getYammerDeviceUsageUserDetail function"
  intro: |
    Get details about Yammer device usage by user.
    
permissions:
  heading: "Permissions"
  intro: |
    One of the following permissions is required to call this API. To learn more, including how to choose permissions, see [Permissions](../../../concepts/permissions_reference.md).
  list:
  - resource: "Delegated (work or school account)"
    permission: "Reports.Read.All"
  - resource: "Delegated (personal Microsoft account)"
    permission: "Not supported."
  - resource: "Application"
    permission: "Reports.Read.All"
section:
  heading: "HTTP request"
  samples:
  - sampleCode: |
      ```http
      GET /reports/getYammerDeviceUsageUserDetail(period='{period_value}')
      GET /reports/getYammerDeviceUsageUserDetail(date={date_value})
      ```
      
  outro: |
    ## Request parameters
    In the request URL, provide the query parameter with a valid value.
    |Parameter|Type|Description
    | :-------- -| :----- -| :--------------------------------------- -|
    |period|string|Specifies the length of time over which the report is aggregated. The supported values for {period_value} are: D7, D30, D90, and D180. These values follow the format D*n* where *n* represents the number of days over which the report is aggregated.
    |date|Date|Specifies the date for which you would like to view the users who performed any activity. {date_value} must have a format of YYYY-MM-DD. As this report is only available for the past 30 days, {date_value} should be a date from that range.
    
requestHeaders:
  heading: "Request headers"
  list:
  - name: "Authorization"
    value: ""
    description: "Bearer {token}. Required."
  - name: "If-None-Match"
    value: ""
    description: "If this request header is included and the eTag provided matches the current tag on the file, a 304 Not Modified response code is returned. Optional."
response:
  heading: "Response"
  intro: |
    If successful, this method returns a `302 Found` response that redirects to a preauthenticated download URL for the report. That URL can be found in the `Location` header in the response.
    Preauthenticated download URLs are only valid for a short period of time (a few minutes) and do not require an `Authorization` header.
    The CSV file has the following headers for columns.
    
    * Report Refresh Date
    * User Principal Name
    * Display Name
    * User State
    * State Change Date
    * Last Activity Date
    * Used Web
    * Used Windows Phone
    * Used Android Phone
    * Used iPhone
    * Used iPad
    * Used Others
    * Report Period
    
