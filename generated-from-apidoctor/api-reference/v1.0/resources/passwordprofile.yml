### YamlMime:GraphResource
uid: graph.passwordprofile
metadata:
  content_git_url: https://github.com/microsoftgraph/microsoft-graph-docs/blob/master/api-reference/v1.0/resources/passwordprofile.md
summary:
  heading: "passwordProfile resource type"
  intro: |
    Contains the password profile associated with a user. The **passwordProfile** property of the [user](xref:graph.user) entity is a **passwordProfile** object.
resource:
  heading: "JSON representation"
  intro: |
    Here is a JSON representation of the resource
  sampleCode: |
    ```json
    {
      "forceChangePasswordNextSignIn": true,
      "password": "string"
    }
    ```
    
properties:
  heading: "Properties"
  list:
  - property: "forceChangePasswordNextSignIn"
    type: "Boolean"
    description: "true if the user must change her password on the next login; otherwise false."
  - property: "password"
    type: "String"
    description: "The password for the user. This property is required when a user is created. It can be updated, but the user will be required to change the password on the next login. The password must satisfy minimum requirements as specified by the user’s passwordPolicies property. By default, a strong password is required."
